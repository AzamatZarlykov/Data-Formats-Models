1) using jq, create a json array of student objects that study in univeristies. 
   Student object contains first and last name and university that they study.

2) using jq, create a json array of professors and their research and paper

3) using jq, create a json array of objects that display what study program every student studies

4) using jq, create a json array of objects that display univeristy name and list of faculties it has.
   Faculties should include the courses` ids they teach and number of credits